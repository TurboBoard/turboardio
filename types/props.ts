import { User } from "@Types";

export type AccountProps = {
    user: User;
};

export type CreateBountyProps = {
    user: User;
};

export type PageProps = {
    content: any;
    title: string;
};
